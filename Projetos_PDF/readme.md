# Bibliotecas python com PIP 

## Imagens para PDF
```shell
pip install openpyxl
pip install PIL
pip install tkinter
```

## Excel para PDF
```shell
pip install openpyxl
pip install reportlab
pip install tkinter
```
